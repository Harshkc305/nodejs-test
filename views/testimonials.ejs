<%- include("./layout/header")%>

<header class="bg-light text-center py-5">
    <div class="container">
      <h1 class="display-4">What Our Clients Say</h1>
      <p class="lead">Real feedback from real clients we've helped succeed.</p>
    </div>
  </header>

  <!-- Testimonial Cards -->
  <section class="py-5">
    <div class="container">
      <div class="row g-4">

        <!-- Testimonial 1 using foreach method -->



         <!-- <% testimonials.forEach(function(t) { %>
        <div class="col-md-4">
          <div class="card shadow-sm border-0 h-100">
            <div class="card-body">

              <p class="card-text"><%=t.massage %></p>
              <div class="d-flex align-items-center mt-4">
                <img src="<%= t.image %>" alt="<%= t.name %>" class="rounded-circle me-3" height="200" width="200">
                <div>
                  <h6 class="mb-0"> <%= t.name%></h6>
                  <small class="text-muted"><%= t.role %></small>
                </div>
              </div>
            </div>
          </div>
        </div>

        <% }); %> -->

<!--    testimonials using map mathod -->


        <% testimonials.map(data=>{ %>
          <div class="col-md-4">
          <div class="card shadow-sm border-0 h-100">
            <div class="card-body">

              <p class="card-text"><%=data.massage %></p>
              <div class="d-flex align-items-center mt-4">
                <img src="<%= data.image %>" alt="<%= data.name %>" class="rounded-circle me-3" height="200" width="200">
                <div>
                  <h6 class="mb-0"> <%= data.name%></h6>
                  <small class="text-muted"><%= data.role %></small>
                </div>
              </div>
            </div>
          </div>
        </div>

        <% }) %>

      </div>
    </div>
  </section>

  <%- include("./layout/footer")%>
